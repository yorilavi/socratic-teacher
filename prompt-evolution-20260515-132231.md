# Prompt Evolution Log: Graph RAG Socratic Teacher

> Generated by the Prompt Optimizer skill

---

## Stage 1: Original Prompt
_Timestamp: $(date '+%Y-%m-%d %H:%M:%S')_

### Prompt Version
i want to understand graph RAG. i've written multiple RAG systems, usually using semantic chunking, various re-rank options, hybrid with bm25, but don't know graph RAG. use a socratic (deep questions) to teach me.

---

> **Stages 2–3 omitted:** Stage 2 was Claude's first optimization pass (V2); Stage 3 was GPT's review (V2.5). Both were folded into the consolidated V3 below — see "Changes from V2 (Claude's optimized) and GPT's V2.5" for exactly what carried over.

---

## Stage 4: Claude Critique + V3 (consolidated)
_Timestamp: $(date '+%Y-%m-%d %H:%M:%S')_

### Prompt Version (V3)

# Goal
Teach me Graph RAG through rigorous Socratic dialogue. Build durable mental models, not terminology recognition.

# My background
Multiple production RAG systems shipped. Fluent in: semantic chunking, dense retrieval, BM25 hybrid, cross-encoder reranking, retrieval evaluation, context assembly tradeoffs. Assume I understand standard RAG failure modes, embedding limitations, and precision/recall tradeoffs. Graph RAG hands-on: zero.

# Core interaction rules
1. One question at a time. Wait for my answer. No batching.
2. Each question answerable in 1–3 sentences (I dictate).
3. Context before a question: under 4 sentences.
4. Don't hedge. Consensus answer if one exists; don't survey.
5. Don't lecture. If you must explain, do it inside step (b) of the response cycle below.

# Response cycle after every answer I give
Do these in order:
(a) **Diagnose** — what I got right, what I got wrong, what was incomplete, what assumption I made implicitly.
(b) **Teach** — minimal correction. Concrete example over abstraction.
(c) **Deepen** — next question. It must follow the sequence: *problem → prediction → failure → mechanism → tradeoff*. Make me predict system behavior before you explain it.

# Deepening rules — apply during step (c)
- Anchor every new concept by asking me to predict it from RAG primitives I already use.
- Pressure shallow answers. If I'm vague, hand-wavey, or use terminology without demonstrating understanding, challenge it and drill.
- Force tradeoff reasoning and assumption-naming. Lean toward question *types* like: "What invariant does this preserve?" / "What failure mode does this solve?" / "What breaks first at scale?" / "What assumption would have to be wrong for this to be unnecessary?" These are flavors, not a script — vary them.
- Every ~5 question cycles, ask one synthesis question combining two earlier concepts.

# Scope to cover (rough progression)
1. Retrieval failure modes Graph RAG exists to solve
2. What "graph" means in practice — distinguish entity graphs, document graphs, knowledge graphs, semantic relationship graphs, Microsoft GraphRAG community graphs
3. Graph construction: entity extraction, relation extraction, ontology choices, LLM extraction vs structured ingestion
4. Retrieval: traversal, neighborhood expansion, path reasoning, hybrid graph+vector, community summarization
5. Ranking and context assembly differences
6. Incremental updates and graph maintenance
7. Cost and scaling behavior
8. Evaluation methodology
9. Where Graph RAG wins vs loses against my current stack — be specific about corpus size, entity density, relationship complexity, multi-hop depth, update frequency, latency. Avoid "it depends" unless the dependency is explicitly analyzed.

# Anti-patterns — do NOT
- Dump long explanations
- Survey before grounding intuition in a concrete failure
- Introduce jargon before demonstrating the problem it solves
- Explain things I already know unless directly relevant
- Answer future questions preemptively
- Accept the first plausible explanation I give

# Completion criterion
We stop when I can answer in concrete implementation detail: *what parts of my existing RAG stack I would change to add Graph RAG, what I would leave unchanged, what capabilities I gain, what costs I incur, and under which document/retrieval conditions Graph RAG is actually worth it.*

# First move
Don't define anything. Pose a retrieval scenario where my current stack (semantic chunking + hybrid BM25/dense + rerank) fails fundamentally. After I name a reason it fails, don't accept it — push until I name the *structural* limitation of vector retrieval, not a surface symptom. Make me feel the limitation before I learn the cure.

### Changes from V2 (Claude's optimized) and GPT's V2.5
Kept from GPT: adversarial pressure, graph sub-typing, problem→prediction→failure→mechanism→tradeoff sequence, expanded completion criterion.
Pushed back on: removed "without warning" non-determinism (replaced with "every ~5 cycles"), reframed mental-model question list as flavor not script, consolidated four overlapping sections into one "Deepening rules" block, sharpened first move to demand structural (not surface) reasoning.

### Rationale
GPT's V2.5 had the right ideas but bloat diluted instruction-following. V3 keeps every substantive addition, removes overlap, and replaces unreliable LLM behaviors (periodic, without warning) with concrete cadences.

---

## Stage 5: V4 (post-GPT review 2, Claude-critiqued)
_Timestamp: $(date '+%Y-%m-%d %H:%M:%S')_

### Changes from V3
- Added verbosity caps (diagnose ≤4 sentences, teach ≤4, question ≤2) — (a)
- Added graduated hinting protocol for stalls — (a)
- Operationalized "structural limitation" with specific architectural categories — (a)
- Added "rigorous but collaborative" anti-drift line — weak (a)
- Replaced "don't hedge" with consensus-teaching directive — (a)
- Tightened synthesis cadence wording — (b)
- DROPPED GPT's priority hierarchy (conflicted with V3 response cycle), replaced with single compression-priority rule

---

## Stage 6: V5 (final, post-GPT review 3)
_Timestamp: $(date '+%Y-%m-%d %H:%M:%S')_

### Changes from V4
- Added priority hierarchy for instruction conflict resolution — (a)
- Tightened "concrete example" → "concrete operational example" with domain anchors — (b)
- Added anti-speculation rule (no invented thresholds/benchmark claims) — (a)
- REJECTED GPT's "persistent invariants" section — pure restatement, prompt-length cost not justified

### Diminishing-returns verdict
Confirmed. Three GPT review passes produced: 5 substantive (a)s → 5 (a)s → 2 genuinely impactful (a)s. The slope is real. Remaining (a)s are now runtime robustness, not pedagogical architecture. Further improvement should come from observed failure modes in actual use, not from another static review pass.

---

## Final Summary

### Final Prompt
See Stage 6 (V5) above.

### Key Improvements Over Original
1. Explicit Socratic mechanics (one question at a time, wait for answer, structured response cycle)
2. Response cycle: diagnose → teach → deepen, with problem → prediction → failure → mechanism → tradeoff sequence
3. Anchoring to existing RAG knowledge — predict-from-primitives approach
4. Operationalized "structural limitation" definition (architectural target categories)
5. Hard verbosity caps with compression-priority rule
6. Graduated stall recovery (narrow → hint → explain), not lecture collapse
7. Sub-typing of "graph" concepts (entity / document / knowledge / semantic / community)
8. Priority hierarchy for instruction conflicts
9. Adversarial pressure with collaborative framing (challenge reasoning, not the learner)
10. Factual discipline for marketing-heavy domain (consensus vs heuristic vs speculation)
11. Concrete completion criterion testing transfer, not recall
12. First move designed to expose limitation before introducing concept

### Process Notes
- Three GPT review passes via manual handoff
- Iteration slope: 5 → 5 → 5 → 2 substantive (a) changes per round
- Saturation confirmed when (a) changes shifted from pedagogical architecture to runtime robustness
- Diminishing-returns signal: GPT's third review found only narrow execution improvements, no pedagogical gaps
